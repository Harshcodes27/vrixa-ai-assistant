services:
 - type: web
 name: vrixa-ai-assistant
 runtime: python
 buildCommand: pip install -r requirements.txt
 startCommand: uvicorn app:app --app-dir New folder --host 0.0.0.0 --port 
 envVars:
 - key: PYTHON_VERSION
 value: 3.11.8
